"""FastAPI HTTP layer wrapping the payout engine.

The frontend (React) talks to this; install with:
    pip install -e ".[api]"
and run with:
    payout-api          # or `uvicorn payout.api.app:app --reload`
"""
