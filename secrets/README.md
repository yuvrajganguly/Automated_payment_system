# Private keys and tokens for this deployment. Never committed (see .gitignore).
# LightsailDefaultKey-ap-south-1.pem  -> SSH key for the Qwikserve-payout instance (user: ubuntu)
# Backup-codes-qwikcustomerhelp.txt  -> Google 2-Step backup codes for qwikcustomerhelp@gmail.com,
#                                       the account that sends password-reset emails (Gmail App
#                                       Password lives only in /root/payout/deploy/.env on the server)
